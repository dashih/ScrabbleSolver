plugins {
    id 'java'
}

group 'org.dannyshih'
version '1.0'

repositories {
    mavenCentral()
}

dependencies {
}

jar {
    manifest {
        attributes 'Main-Class': 'ScrabbleSolver'
    }
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }}
}